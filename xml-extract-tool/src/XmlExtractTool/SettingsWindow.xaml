<Window
    x:Class="XmlExtractTool.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="设置"
    Width="520"
    Height="480"
    WindowStartupLocation="CenterOwner"
    ResizeMode="CanResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,4" Text="读取文件格式（每行一个扩展名，如 .mil .upe .xml .uff）" />
        <TextBox
            Grid.Row="1"
            Margin="0,0,0,12"
            AcceptsReturn="True"
            Text="{Binding FileExtensions, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />

        <TextBlock Grid.Row="2" Margin="0,0,0,4" Text="LoopMode=2 不循环（每行一个文件名关键词，* 表示任意字符）" />
        <TextBox
            Grid.Row="3"
            Margin="0,0,0,12"
            AcceptsReturn="True"
            Text="{Binding KeywordsLoopMode2, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />

        <TextBlock Grid.Row="4" Margin="0,0,0,4" Text="LoopMode=1 循环（每行一个文件名关键词）" />
        <TextBox
            Grid.Row="5"
            Margin="0,0,0,12"
            AcceptsReturn="True"
            Text="{Binding KeywordsLoopMode1, UpdateSourceTrigger=PropertyChanged}"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="0,0,8,0" Click="OkButton_OnClick" Content="确定" IsDefault="True" />
            <Button Width="80" Content="取消" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
