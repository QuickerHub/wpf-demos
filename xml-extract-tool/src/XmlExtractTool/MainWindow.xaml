<Window
    x:Class="XmlExtractTool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:XmlExtractTool"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:XmlExtractTool.ViewModels"
    Title="XML 四元数检测工具"
    Width="800"
    Height="600"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="600"
    d:DesignWidth="800"
    Background="{DynamicResource DefaultWindowBackgroundBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </Window.Resources>
    <Grid Margin="16" Background="{DynamicResource RegionBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="24"
            FontWeight="Bold"
            Text="XML 四元数检测工具" />

        <!-- File Selection Area -->
        <GroupBox
            Grid.Row="1"
            Margin="0,0,0,16"
            Header="文件选择">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    Grid.Column="0"
                    Margin="0,0,8,0"
                    IsReadOnly="True"
                    Text="{Binding ViewModel.FilePath, Mode=OneWay}" />
                <Button
                    Grid.Column="1"
                    Command="{Binding ViewModel.SelectFileCommand}"
                    Content="选择文件" />
            </Grid>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,16"
            Orientation="Horizontal">
            <Button
                Margin="0,0,8,0"
                Command="{Binding ViewModel.CheckQuaternionsCommand}"
                Content="开始检测"
                IsEnabled="{Binding ViewModel.IsChecking, Converter={StaticResource InverseBooleanConverter}}" />
            <Button
                Margin="0,0,8,0"
                Command="{Binding ViewModel.CopyResultsCommand}"
                Content="复制结果" />
            <Button
                Command="{Binding ViewModel.SaveResultsCommand}"
                Content="保存结果" />
        </StackPanel>

        <!-- Results Area -->
        <GroupBox
            Grid.Row="3"
            Margin="0,0,0,16"
            Header="检测结果">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Margin="0,0,0,8"
                    Text="{Binding ViewModel.StatusMessage, Mode=OneWay}" />
                <ListBox
                    Grid.Row="1"
                    ItemsSource="{Binding ViewModel.InvalidNodeNames}"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </GroupBox>

        <!-- Status Bar -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                Text="{Binding ViewModel.StatusMessage, Mode=OneWay}" />
            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal">
                <TextBlock
                    Margin="8,0,0,0"
                    Text="总节点数: " />
                <TextBlock Text="{Binding ViewModel.TotalNodes, Mode=OneWay}" />
                <TextBlock
                    Margin="16,0,0,0"
                    Text="不符合条件: " />
                <TextBlock Text="{Binding ViewModel.InvalidNodesCount, Mode=OneWay}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
