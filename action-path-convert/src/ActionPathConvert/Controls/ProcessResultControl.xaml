<UserControl
    x:Class="ActionPathConvert.Controls.ProcessResultControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:controls="clr-namespace:ActionPathConvert.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Left: File Editor  -->
        <!--<GroupBox Grid.Column="0" Header="文件内容">
        </GroupBox>-->
        <controls:FileEditorControl x:Name="FileEditor" FilePath="{Binding ProcessResult.M3uFilePath, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <!--  Splitter  -->
        <GridSplitter
            Grid.Column="1"
            HorizontalAlignment="Stretch"
            Background="{DynamicResource BorderBrush}" />

        <!--  Right: Not Found Files  -->
        <Border
            Grid.Column="2"
            Background="{DynamicResource RegionBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock
                    Grid.Row="0"
                    Margin="8,4"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="不存在的文件" />
                <avalonedit:TextEditor
                    x:Name="NotFoundFilesEditor"
                    Grid.Row="1"
                    Background="{DynamicResource RegionBrush}"
                    BorderThickness="0"
                    FontFamily="Consolas"
                    FontSize="12"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    IsReadOnly="True"
                    ShowLineNumbers="True"
                    WordWrap="False" />
            </Grid>
        </Border>
    </Grid>
</UserControl>

