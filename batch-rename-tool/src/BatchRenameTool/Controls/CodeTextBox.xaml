<UserControl
    x:Class="BatchRenameTool.Controls.CodeTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:codeCompletion="clr-namespace:ICSharpCode.AvalonEdit.CodeCompletion;assembly=ICSharpCode.AvalonEdit"
    x:Name="Root">
    <UserControl.Resources>
        <Style TargetType="{x:Type codeCompletion:CompletionWindow}">
            <Setter Property="WindowStyle" Value="None" />
            <Setter Property="ResizeMode" Value="NoResize" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type codeCompletion:CompletionWindow}">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="0">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  AvalonEdit CompletionList Style - Remove border  -->
        <Style TargetType="{x:Type codeCompletion:CompletionList}">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>
    </UserControl.Resources>
    <Border
        Background="{DynamicResource RegionBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1">
        <avalonedit:TextEditor
            x:Name="TextEditor"
            Padding="2"
            Background="{DynamicResource RegionBrush}"
            BorderThickness="0"
            FontFamily="Consolas"
            Foreground="{DynamicResource PrimaryTextBrush}"
            ShowLineNumbers="False"
            WordWrap="False" />
    </Border>
</UserControl>
