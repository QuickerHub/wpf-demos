<Window
    x:Class="BatchRenameTool.Controls.InputDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:BatchRenameTool.Controls"
    Title="输入"
    Width="500"
    MinHeight="120"
    Background="{DynamicResource RegionBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    ResizeMode="NoResize"
    SizeToContent="Height"
    WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Title  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,15"
            FontSize="14"
            FontWeight="SemiBold"
            Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" />

        <!--  Input  -->
        <controls:TemplateInputBox
            x:Name="InputBox"
            Grid.Row="1"
            Margin="0,0,0,15"
            MinHeight="30"
            Text="{Binding InputText, RelativeSource={RelativeSource AncestorType=Window}, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />

        <!--  Buttons  -->
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="0,0,10,0"
                Padding="20,5"
                Click="ConfirmButton_Click"
                Content="确认"
                IsDefault="True" />
            <Button
                Padding="20,5"
                Click="CancelButton_Click"
                Content="取消" />
        </StackPanel>
    </Grid>
</Window>
