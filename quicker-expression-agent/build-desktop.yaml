# BuildTools 简化配置文件
# 将此文件放在你的项目根目录，然后运行 BuildTools.exe 即可自动构建

# 基本项目信息
name: quicker-expression-agent.desktop
displayName: QuickerExpressionAgent.Desktop
versionKey: QuickerExpressionAgent.Desktop          # 对应 version.json 中的键名
subprogramName: QuickerExpressionAgent.Desktop
# 项目路径（可选，默认为当前目录）
projectDir: .

# 发布相关配置
uploadDir: cea.quicker-expression-agent.desktop
packageName: cea.quicker-expression-agent.desktop
instruction: Quicker Expression Agent Desktop Application

# 混淆配置（可选）
obfuscate: true
toConfuse:                    # 要混淆的文件（当 obfuscate: true 时）
  - "QuickerExpressionAgent.Server.dll"
copyAllFiles: true
# 测试配置（可选）
testDir: "Quicker/_packages/cea.quicker-expression-agent.desktop"

# 构建控制
enabled: true

