<UserControl
    x:Class="QuickerExpressionAgent.Desktop.Controls.CodeTextControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    x:Name="Root">
    <UserControl.Resources>
        <!-- Custom style to remove AvalonEdit's built-in ScrollViewer -->
        <!-- This allows parent ScrollViewer to handle mouse wheel events -->
        <Style TargetType="{x:Type avalonedit:TextEditor}" x:Key="NoScrollTextEditor">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
            <Setter Property="FlowDirection" Value="LeftToRight"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type avalonedit:TextEditor}">
                        <ContentPresenter 
                            Focusable="False"
                            Content="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TextArea}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border
        BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
        BorderThickness="1"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
        <avalonedit:TextEditor
            x:Name="TextEditor"
            Style="{StaticResource NoScrollTextEditor}"
            Background="Transparent"
            BorderThickness="0"
            FontFamily="Consolas"
            FontSize="12"
            ShowLineNumbers="False"
            WordWrap="False"
            Padding="2" />
    </Border>
</UserControl>
