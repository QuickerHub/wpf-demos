# WpfAppForQuicker

最小 Quicker + WPF net472 示例。Quicker 程序集由仓库根 `Directory.Build.props` → `qkref.props` 自动引用。

```powershell
dotnet build
# 或在 IDE 中打开 WpfAppForQuicker.csproj / WpfAppForQuicker.slnx
```

完整脚手架与 Release 双输出模式见 [quicker-workspace](https://github.com/QuickerHub/quicker-workspace)。
