<UserControl
    x:Class="QuickerCodeEditor.View.CodeEditor.CodeEditorStateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:codeEditor="clr-namespace:QuickerCodeEditor.View.CodeEditor"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:view="clr-namespace:QuickerCodeEditor.View"
    Name="TheControl"
    Background="{DynamicResource RegionBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}">
    <UserControl.Resources>
        <codeEditor:EqualityToVisibilityConverter x:Key="EqualityToVisibilityConverter" />
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="MaxHeight" Value="80" />
            <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick" />
            <Setter Property="ContextMenu">
                <Setter.Value>
                    <ContextMenu>
                        <MenuItem
                            Command="{Binding ViewModel.DeleteStateCommand, Source={x:Reference TheControl}}"
                            CommandParameter="{Binding}"
                            Header="删除" />
                    </ContextMenu>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border
                            x:Name="Bd"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThirdlyRegionBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListBox
            x:Name="ListBox"
            Grid.Row="0"
            dd:DragDrop.IsDragSource="True"
            dd:DragDrop.IsDropTarget="True"
            AllowDrop="True"
            ItemsSource="{Binding ViewModel.States}"
            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
            SelectedItem="{Binding ViewModel.SelectedState, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type view:CodeEditorState}">
                    <StackPanel>
                        <Grid>
                            <TextBlock
                                x:Name="NameTextBlock"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource PrimaryTextBrush}"
                                Text="{Binding Name}"
                                TextWrapping="Wrap">
                                <TextBlock.Visibility>
                                    <MultiBinding Converter="{StaticResource EqualityToVisibilityConverter}" ConverterParameter="Inverse">
                                        <Binding Path="ViewModel.EditingState" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        <Binding />
                                    </MultiBinding>
                                </TextBlock.Visibility>
                            </TextBlock>
                            <TextBox
                                x:Name="NameTextBox"
                                Padding="2"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                FontSize="13"
                                FontWeight="SemiBold"
                                IsVisibleChanged="NameTextBox_IsVisibleChanged"
                                KeyDown="NameTextBox_KeyDown"
                                Loaded="NameTextBox_Loaded"
                                LostFocus="NameTextBox_LostFocus"
                                Text="{Binding ViewModel.EditingName, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}">
                                <TextBox.Visibility>
                                    <MultiBinding Converter="{StaticResource EqualityToVisibilityConverter}">
                                        <Binding Path="ViewModel.EditingState" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        <Binding />
                                    </MultiBinding>
                                </TextBox.Visibility>
                            </TextBox>
                        </Grid>
                        <TextBlock
                            MaxHeight="45"
                            Margin="0,2,0,0"
                            FontSize="11"
                            Foreground="{DynamicResource SecondaryTextBrush}"
                            LineHeight="15"
                            Text="{Binding Expression}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!--  New Button  -->
        <Button
            Grid.Row="1"
            Margin="0,8,0,0"
            Padding="12,10"
            HorizontalAlignment="Stretch"
            HorizontalContentAlignment="Left"
            Background="Transparent"
            BorderThickness="0"
            Command="{Binding ViewModel.AddStateCommand}"
            Cursor="Hand"
            FontSize="13"
            FontWeight="SemiBold">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock
                        Margin="0,0,8,0"
                        VerticalAlignment="Center"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="{DynamicResource SecondaryTextBrush}"
                        Text="+" />
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        Text="新建表达式" />
                </StackPanel>
            </Button.Content>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border
                                    x:Name="Border"
                                    Padding="{TemplateBinding Padding}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource SecondaryRegionBrush}" />
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="{DynamicResource ThirdlyRegionBrush}" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>

