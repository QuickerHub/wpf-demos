<UserControl x:Class="WindowAttach.Controls.SettingsSelectorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="CheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBoxBorder"
                                    Width="16" Height="16" 
                                    BorderBrush="#80FFFFFF" 
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Background="Transparent"
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M 3,7 L 7,11 L 13,3"
                                      Stroke="White"
                                      StrokeThickness="2"
                                      StrokeStartLineCap="Round"
                                      StrokeEndLineCap="Round"
                                      StrokeLineJoin="Round"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#B0FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <StackPanel Orientation="Vertical" Margin="4">
        <CheckBox x:Name="RestrictToSameScreenCheckBox"
                  Content="限制在屏幕内"
                  Style="{StaticResource CheckBoxStyle}"
                  Checked="CheckBox_Changed"
                  Unchecked="CheckBox_Changed"/>
        
        <CheckBox x:Name="AutoAdjustToScreenCheckBox"
                  Content="自动调整吸附位置"
                  Style="{StaticResource CheckBoxStyle}"
                  Checked="CheckBox_Changed"
                  Unchecked="CheckBox_Changed"/>
    </StackPanel>
</UserControl>

