<Window x:Class="WindowAttach.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WindowAttach.ViewModels"
        Title="窗口测试工具" 
        Width="800" 
        Height="600"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:WindowTestViewModel />
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Toolbar with buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button 
                x:Name="PickWindowButton"
                Content="选择窗口" 
                Padding="15,8"
                FontSize="14"
                Cursor="Hand"
                PreviewMouseLeftButtonDown="PickWindowButton_PreviewMouseLeftButtonDown"
                GiveFeedback="PickWindowButton_GiveFeedback"
                Margin="0,0,10,0"/>
            <Button 
                Content="刷新属性" 
                Command="{Binding RefreshPropertiesCommand}"
                Padding="15,8"
                FontSize="14"
                Cursor="Hand"
                Margin="0,0,10,0"/>
            <Button 
                Content="测试窗口吸附" 
                Click="TestWindowAttach_Click"
                Padding="15,8"
                FontSize="14"
                Cursor="Hand"/>
        </StackPanel>

        <!-- Status message -->
        <TextBlock 
            Grid.Row="1"
            Text="{Binding StatusMessage}" 
            FontSize="14" 
            Foreground="Gray"
            Margin="0,0,0,10"
            TextWrapping="Wrap"/>
        
        <!-- Window properties display -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Properties}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,2">
                            <TextBlock 
                                Text="{Binding Name, StringFormat='{}{0}:'}" 
                                FontWeight="SemiBold"
                                Width="150"
                                VerticalAlignment="Center"/>
                            <TextBlock 
                                Text="{Binding Value}" 
                                VerticalAlignment="Center"
                                TextWrapping="Wrap"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
