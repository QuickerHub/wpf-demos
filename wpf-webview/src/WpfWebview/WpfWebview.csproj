<Project Sdk="Microsoft.NET.Sdk">
	<!-- Import Quicker WPF build configuration -->
	<Import Project="QuickerWpf.Build.props" />

	<!-- Web project configuration -->
	<PropertyGroup>
		<WebProjectPath>$(MSBuildThisFileDirectory)..\WpfWebview.Web</WebProjectPath>
	</PropertyGroup>

	<!-- Import WebView build configuration -->
	<Import Project="WebView.Build.props" />
</Project>
