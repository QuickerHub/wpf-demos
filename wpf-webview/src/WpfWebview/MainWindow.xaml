<Window
    x:Class="WpfWebview.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfWebview"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
    Title="WPF WebView 交互演示"
    Width="1200"
    Height="800"
    d:DataContext="{d:DesignInstance local:MainWindow,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    Background="{DynamicResource DefaultWindowBackgroundBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Toolbar  -->
        <Border
            Grid.Row="0"
            Padding="10"
            Background="{DynamicResource SecondaryRegionBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button
                    Margin="5,0"
                    Padding="10,5"
                    Command="{Binding ViewModel.GoBackCommand}"
                    Content="← 后退" />
                <Button
                    Margin="5,0"
                    Padding="10,5"
                    Command="{Binding ViewModel.GoForwardCommand}"
                    Content="前进 →" />
                <Button
                    Margin="5,0"
                    Padding="10,5"
                    Command="{Binding ViewModel.ReloadPageCommand}"
                    Content="🔄 刷新" />
                <Button
                    Margin="5,0"
                    Padding="10,5"
                    Command="{Binding ViewModel.NavigateToUrlCommand}"
                    Content="🌐 导航" />
                <Separator Margin="10,0" />
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Text="{Binding ViewModel.CurrentUrl}"
                    TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>

        <!--  Control Panel  -->
        <Border
            Grid.Row="1"
            Padding="15"
            Background="{DynamicResource ThirdlyRegionBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Send to Web  -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock
                        Margin="0,0,0,5"
                        FontWeight="Bold"
                        Text="发送消息到网页:" />
                    <StackPanel Orientation="Horizontal">
                        <TextBox
                            Width="300"
                            Margin="0,0,5,0"
                            Padding="5"
                            Text="{Binding ViewModel.MessageToWeb, UpdateSourceTrigger=PropertyChanged}" />
                        <Button
                            Padding="15,5"
                            Command="{Binding ViewModel.SendMessageToWebCommand}"
                            Content="发送" />
                    </StackPanel>
                </StackPanel>

                <!--  Receive from Web  -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock
                        Margin="0,0,0,5"
                        FontWeight="Bold"
                        Text="来自网页的消息:" />
                    <Border
                        MinHeight="40"
                        Padding="10"
                        Background="{DynamicResource RegionBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1">
                        <TextBlock Text="{Binding ViewModel.MessageFromWeb}" TextWrapping="Wrap" />
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!--  WebView  -->
        <Border
            Grid.Row="2"
            Background="{DynamicResource RegionBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,1,0,0">
            <Grid>
                <wv2:WebView2 x:Name="WebViewControl" />

                <!--  Loading overlay  -->
                <Border
                    Background="{DynamicResource RegionBrush}"
                    Opacity="0.95"
                    Visibility="{Binding ViewModel.IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Orientation="Vertical">
                        <!--  Loading progress bar  -->
                        <ProgressBar
                            Width="200"
                            Height="4"
                            Margin="0,0,0,20"
                            IsIndeterminate="True" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!--  Status Bar  -->
        <Border
            Grid.Row="3"
            Padding="10,5"
            Background="{DynamicResource SecondaryRegionBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,1,0,0">
            <TextBlock FontSize="12" Text="就绪" />
        </Border>
    </Grid>
</Window>

